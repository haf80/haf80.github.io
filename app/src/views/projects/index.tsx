import React, { FunctionComponent, ReactElement } from "react";
import App from "../../components/projects/index";

const Projects: FunctionComponent = (): ReactElement => {
  return <App />;
};
export default Projects;
